/*
 * This is sample code generated by rpcgen.
 * These are only templates and you can use them
 * as a guideline for developing your own functions.
 */

#include "u19cs009.h"


void
exec_prog_1(char *host)
{
	CLIENT *clnt;
	output  *result_1;
	input  exec_1_arg;

#ifndef	DEBUG
	clnt = clnt_create (host, EXEC_PROG, EXEC_VERS, "udp");
	if (clnt == NULL) {
		clnt_pcreateerror (host);
		exit (1);
	}
#endif	/* DEBUG */
	printf("Please enter a number : ");
	scanf("%d", &exec_1_arg.num);
	printf("Number is : %d\n", exec_1_arg.num);
	
	result_1 = exec_1(&exec_1_arg, clnt);
	if (result_1 == (output *) NULL) {
		clnt_perror (clnt, "call failed");
	}
#ifndef	DEBUG
	clnt_destroy (clnt);
#endif	 /* DEBUG */
}


int
main (int argc, char *argv[])
{
	char *host;

	if (argc < 2) {
		printf ("usage: %s server_host\n", argv[0]);
		exit (1);
	}
	host = argv[1];
	exec_prog_1 (host);
exit (0);
}
